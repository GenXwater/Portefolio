# SMTP configuration for the Netlify function (used locally or as reference)
NETLIFY_SMTP_HOST=smtp.example.com
NETLIFY_SMTP_PORT=587
NETLIFY_SMTP_SECURE=false
NETLIFY_SMTP_USER=your_smtp_user
NETLIFY_SMTP_PASS=your_smtp_password
EMAIL_TO=you@example.com

# Notes:
# - Sur Netlify, définir ces variables via Site settings > Build & deploy > Environment.
# - Pour tester localement les fonctions Netlify, utilisez netlify-cli (netlify dev) ou un outil équivalent.
